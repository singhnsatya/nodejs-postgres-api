It requires nodejs version 8 and later.

node api setup:

cd nodejs-postgres-api

1. npm intall
2. npm install -g knex
3. createdb userforms
4. knex migrate:latest -> this will create table in your database

